[package]
name = "notas-core"
version.workspace = true
edition.workspace = true
description = "Core data layer for Notas: schema, repository, search (no GTK)"

[dependencies]
thiserror.workspace = true
sqlx.workspace = true
tokio.workspace = true
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
